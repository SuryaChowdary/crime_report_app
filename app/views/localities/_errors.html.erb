<% if @locality.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@locality.errors.count, "error") %> prohibited this locality reporter from being saved:</h2>
    <ul>
      <% @locality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>