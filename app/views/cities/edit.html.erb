<h1> Edit City Details </h1>

  <%= form_with(model: @city, local: true) do |f| %>

  <p> 
    <%= f.label :Name %> <br/>
    <%= f.text_area :name %>
  </p>

  <p>
    <%= f.submit "Update City name" %>
  </p>

<% end %>